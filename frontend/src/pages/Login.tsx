import { useState } from "react";
import { LockKeyhole, LogIn, UserPlus } from "lucide-react";
import { Link, useNavigate } from "react-router-dom";
import { useAuth } from "../store/AuthContext";

export default function Login() { const navigate = useNavigate(); const { login } = useAuth(); const [email, setEmail] = useState(""); const [password, setPassword] = useState(""); const [error, setError] = useState(""); const [busy, setBusy] = useState(false);
  async function submit(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); setBusy(true); setError(""); try { const user = await login(email, password); navigate(user.role === "ADMIN" ? "/admin" : "/", { replace: true }); } catch { setError("Invalid credentials or API unavailable."); } finally { setBusy(false); } }
  return <main className="standalone-auth"><section className="auth-intro"><p className="eyebrow">FloodFlow access</p><h1>Welcome to FloodFlow</h1><p className="muted wide-copy">Log in to view urban flood forecasts, plan safer routes, and submit observations.</p></section><form className="form-card auth-card" onSubmit={submit}><div className="auth-icon"><LockKeyhole size={22} /></div><h2>Log in</h2><label>Email<input type="email" value={email} onChange={(event) => setEmail(event.target.value)} placeholder="you@example.com" required /></label><label>Password<input type="password" value={password} onChange={(event) => setPassword(event.target.value)} placeholder="Your password" required /></label><button className="play-button" disabled={busy}><LogIn size={16} />{busy ? "Logging in…" : "Log in"}</button>{error && <p className="form-error">{error}</p>}<div className="auth-links"><span>New public user?</span><Link to="/register"><UserPlus size={15} /> Create account</Link></div><p className="demo-credentials"><b>Administrative access</b><br />Log in with the administrator credentials configured in your environment.</p></form></main>; }
