import { useState } from "react";
import { ArrowLeft, UserPlus } from "lucide-react";
import { Link, useNavigate } from "react-router-dom";
import { useAuth } from "../store/AuthContext";

export default function Register() { const navigate = useNavigate(); const { register } = useAuth(); const [name, setName] = useState(""); const [email, setEmail] = useState(""); const [password, setPassword] = useState(""); const [confirm, setConfirm] = useState(""); const [error, setError] = useState(""); const [busy, setBusy] = useState(false);
  async function submit(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); if (password !== confirm) { setError("Passwords do not match."); return; } setBusy(true); setError(""); try { await register(name, email, password); navigate("/", { replace: true }); } catch { setError("Could not create the public account. Check the details and try again."); } finally { setBusy(false); } }
  return <main className="standalone-auth"><section className="auth-intro"><p className="eyebrow">Public access</p><h1>Create your profile</h1><p className="muted wide-copy">Create a public FloodFlow account to personalise your profile and contribute flood observations.</p></section><form className="form-card auth-card" onSubmit={submit}><div className="auth-icon"><UserPlus size={22} /></div><h2>Create public account</h2><label>Username<input value={name} onChange={(event) => setName(event.target.value)} placeholder="Your display name" required /></label><label>Email<input type="email" value={email} onChange={(event) => setEmail(event.target.value)} placeholder="you@example.com" required /></label><label>Password<input type="password" value={password} onChange={(event) => setPassword(event.target.value)} minLength={8} required /></label><label>Confirm password<input type="password" value={confirm} onChange={(event) => setConfirm(event.target.value)} minLength={8} required /></label><button className="play-button" disabled={busy}><UserPlus size={16} />{busy ? "Creating…" : "Create account"}</button>{error && <p className="form-error">{error}</p>}<Link className="back-link" to="/login"><ArrowLeft size={15} /> Already have an account? Log in</Link><p className="form-note">Administrative accounts are created separately and cannot be registered here.</p></form></main>; }
